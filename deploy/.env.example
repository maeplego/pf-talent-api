TALENT_DATABASE_URL=postgres://talent:talent@localhost:5436/talent
TALENT_DEV_AUTH=true
CALENDAR_INTERNAL_TOKEN=dev-internal-token
CALENDAR_API_URL=http://localhost:8095
# Overlay C では OIDC_ISSUER をセットし、TALENT_DEV_AUTH=true のままにする。
# cluster-smoke は X-Dev-User-Sub を使う。web だけ OIDC 必須。
# OIDC_ISSUER=http://localhost:8080
# OIDC_INTERNAL_BASE=http://host.docker.internal:8080
# OIDC_AUDIENCE=pf-talent-web
